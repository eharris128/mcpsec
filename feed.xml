---
layout: null
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <generator uri="https://jekyllrb.com/" version="{{ jekyll.version }}">Jekyll</generator>
  <link href="{{ site.url }}/feed.xml" rel="self" type="application/atom+xml" />
  <link href="{{ site.url }}/" rel="alternate" type="text/html" />
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ site.url }}/feed.xml</id>
  <title type="html">{{ site.title | xml_escape }}</title>
  <subtitle>{{ site.description | xml_escape }}</subtitle>
  <author>
    <name>{{ site.author | xml_escape }}</name>
  </author>
  {% for advisory in site.advisories reversed limit:20 %}
  <entry>
    <title type="html">{{ advisory.title | xml_escape }}</title>
    <link href="{{ site.url }}{{ advisory.url }}" rel="alternate" type="text/html" title="{{ advisory.title | xml_escape }}" />
    <published>{{ advisory.date | date_to_xmlschema }}</published>
    <updated>{{ advisory.date | date_to_xmlschema }}</updated>
    <id>{{ site.url }}{{ advisory.url }}</id>
    <content type="html" xml:base="{{ site.url }}{{ advisory.url }}">{{ advisory.content | xml_escape }}</content>
    <author>
      <name>{{ site.author | xml_escape }}</name>
    </author>
    {% if advisory.categories %}
    {% for category in advisory.categories %}
    <category term="{{ category | xml_escape }}" />
    {% endfor %}
    {% endif %}
    {% if advisory.excerpt %}
    <summary type="html">{{ advisory.excerpt | xml_escape }}</summary>
    {% endif %}
  </entry>
  {% endfor %}
</feed>
